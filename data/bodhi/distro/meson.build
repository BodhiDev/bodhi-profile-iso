dir = 'distro'
bodhi_data = join_paths(dir_prefix, 'share/bodhi')
install_data([ 'startupcommands' ],
             install_dir  : join_paths(bodhi_data, dir),
             install_mode : 'rwxr--r--'
            )
