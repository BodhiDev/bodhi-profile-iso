dir = 'lightdm.conf.d'
lightdm_data = join_paths(dir_prefix, 'share/lightdm')
install_data([ '50-bodhi.conf'
             ],
             install_dir  : join_paths(lightdm_data, dir),
             install_mode : 'rw-r--r--'
            )
